# 📋 Pickid 테스트 가이드

## 개요

이 문서는 Pickid 프로젝트의 테스트 전략과 실행 방법을 설명합니다.

## 테스트 목적

리팩토링이나 기능 개발 후 QA를 자동화하여 모든 페이지와 컴포넌트가 정상적으로 작동하는지 확인합니다.

## 테스트 환경

- **프레임워크**: Jest
- **테스팅 라이브러리**: React Testing Library
- **커버리지 목표**: 70% (branches, functions, lines, statements)

## 테스트 실행

### 전체 테스트 실행

```bash
# 루트에서 모든 앱의 테스트 실행
pnpm test

# Web 앱만 테스트
cd apps/web && pnpm test

# Admin 앱만 테스트
cd apps/admin && pnpm test
```

### Watch 모드로 테스트 실행

```bash
pnpm test:watch
```

### 커버리지 리포트 생성

```bash
pnpm test:coverage
```

## 테스트 구조

```
apps/web/src/
├── features/
│   └── home/
│       └── ui/
│           └── __tests__/
│               └── balance-game-section.test.tsx
└── __tests__/
    └── integration/
```

## 작성된 테스트

### 1. 밸런스게임 섹션 테스트 ✅

**파일**: `apps/web/src/features/home/ui/__tests__/balance-game-section.test.tsx`

**테스트 케이스**:

- ✅ 밸런스게임 섹션이 올바르게 렌더링된다
- ✅ 밸런스게임 옵션들이 올바르게 렌더링된다
- ✅ 옵션을 클릭하면 결과 화면이 표시된다
- ✅ 투표 결과에서 통계가 올바르게 표시된다
- ✅ 다시 참여하기 버튼이 올바르게 동작한다
- ✅ 투표 시 로컬 스토리지에 데이터가 저장된다
- ✅ 더 많은 밸런스게임 버튼이 올바르게 렌더링된다
- ✅ 주간별로 다른 밸런스게임이 표시된다
- ✅ 이모지와 아이콘이 올바르게 표시된다
- ✅ HOT 배지가 올바르게 표시된다

**실행 결과**: ✅ 10/10 passed

## 테스트 작성 가이드

### 1. 단위 테스트 작성 규칙

```tsx
describe('컴포넌트명', () => {
	beforeEach(() => {
		// 각 테스트 전에 실행할 코드
		jest.clearAllMocks();
	});

	it('기능을 명확하게 설명하는 테스트 이름', () => {
		// Given: 테스트 준비
		render(<Component />);

		// When: 동작 실행
		const button = screen.getByText('버튼');
		fireEvent.click(button);

		// Then: 결과 검증
		expect(screen.getByText('결과')).toBeInTheDocument();
	});
});
```

### 2. 네이밍 컨벤션

- 테스트 파일: `*.test.tsx` 또는 `*.spec.tsx`
- 테스트 디렉토리: `__tests__/`
- describe: 컴포넌트명 또는 기능명
- it: "~한다" 형식으로 한글로 작성

### 3. Mock 사용

jest.setup.js에서 전역 Mock이 설정되어 있습니다:

- localStorage
- next/image
- next/navigation
- @tanstack/react-query
- @pickid/supabase

## 테스트 커버리지 목표

| 항목       | 목표 | 현재 |
| ---------- | ---- | ---- |
| Branches   | 70%  | -    |
| Functions  | 70%  | -    |
| Lines      | 70%  | -    |
| Statements | 70%  | -    |

## 다음 단계

### E2E 테스트 (향후 계획)

- Playwright를 사용한 E2E 테스트 설정
- 주요 사용자 시나리오 테스트
- 크로스 브라우저 테스트

### CI/CD 통합 (향후 계획)

- GitHub Actions에 테스트 자동화
- PR 시 자동 테스트 실행
- 테스트 실패 시 머지 차단

## 문제 해결

### Jest 설정 경고

```
Unknown option "moduleNameMapping" was found.
```

**해결**: 이 경고는 Next.js의 Jest 설정에서 발생하는 것으로 기능에는 영향이 없습니다.

### 테스트 실패 시

1. 테스트 로그 확인
2. 컴포넌트 변경사항 확인
3. Mock 데이터 업데이트 필요 여부 확인
4. 의존성 버전 충돌 확인

## 추가 리소스

- [Jest 공식 문서](https://jestjs.io/)
- [React Testing Library 공식 문서](https://testing-library.com/react)
- [Testing Best Practices](https://kentcdodds.com/blog/common-mistakes-with-react-testing-library)

